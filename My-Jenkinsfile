pipeline {
    // add your slave label name
    agent { label 'my_first_jenkin_slave'}
    tools{
        maven 'maven-test'
    }
    stages {
        stage ('Checkout_SCM') {

            steps {
          	    
	     checkout scm
            }
        }

        stage ('Maven_Build') {

            steps {
               sh 'mvn clean package'
            }
        }
        
        stage ('Deploy_Tomcat') {

            steps {
	      sshagent(['my-web-application-server-key']) {
              sh "scp -o StrictHostKeyChecking=no  target/maven-web-application.war  ec2-user@15.206.122.5:/opt/tomcat9/webapps"
	      }
         }
        }
        
    }
}
